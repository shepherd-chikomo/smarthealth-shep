import 'package:flutter/material.dart';

/// Booking flow scaffold for future payments + tele-consult.
class BookingFlowScaffold extends StatelessWidget {
  const BookingFlowScaffold({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Booking flow — scaffold')),
    );
  }
}
