import 'package:flutter/material.dart';

/// Search filter sheet placeholder.
class SearchFilters extends StatelessWidget {
  const SearchFilters({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
