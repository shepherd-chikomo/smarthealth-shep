<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Local dev API over HTTP (LAN IP or adb reverse localhost). -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
